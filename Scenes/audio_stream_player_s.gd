extends AudioStreamPlayer

func _ready() -> void:

	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	#var moving_audio = $"AudioStreamPlayer-moving"
	#if GameState.is_moving && moving_audio.playing == false:
		#moving_audio.play()
	# if	!GameState.is_moving && moving_audio.playing == true:
	#	moving_audio.stop()
	pass
